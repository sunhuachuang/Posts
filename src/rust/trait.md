### Trait

#### 常见Trait
##### Default
```
impl Default for Kind {
    fn default() -> Kind { Kind::A }
}

```

##### From
```
impl From for Kind {
    fn from() -> Kind { Kind::A }
}

//when use
"xxx".into()

```
